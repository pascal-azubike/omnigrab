# OmniGrab Android — Python requirements for LOCAL DEVELOPMENT
# Install with: pip install -r requirements.txt
# NOTE: These are NOT the same as pyproject.toml requires (those are for the APK).
#       These are for running the server on your desktop to test the UI.

briefcase>=0.3.18
toga-android==0.4.7
fastapi>=0.115.0
uvicorn>=0.30.0
yt-dlp>=2024.12.13
aiofiles>=23.0.0
httpx>=0.27.0
python-multipart>=0.0.9
anyio>=4.0.0
