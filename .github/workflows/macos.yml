name: macOS Build
on:
  push:
    branches: [ main ]
    tags:
      - 'v*'
    paths:
      - 'packages/ui/**'
      - 'packages/desktop/**'
      - '.github/workflows/macos.yml'
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: macos-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      
      - name: setup node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          
      - name: install Rust stable
        uses: dtolnay/rust-toolchain@stable
        
      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: packages/desktop/src-tauri -> target
        
      - name: install project dependencies
        run: |
          rm -f package-lock.json
          npm install
        
      - name: build UI
        run: npm run build
        working-directory: packages/ui

      - name: Download yt-dlp + ffmpeg binaries
        run: node scripts/download-binaries.js
        working-directory: packages/desktop
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Verify sidecar placement
        run: ls -R packages/desktop/src-tauri
        
      - name: Build Desktop Application (non-tag)
        if: "!startsWith(github.ref, 'refs/tags/')"
        run: npm run build
        working-directory: packages/desktop

      - name: Upload macOS Build Artifacts
        if: "!startsWith(github.ref, 'refs/tags/')"
        uses: actions/upload-artifact@v4
        with:
          name: omnigrab-macos-x64
          path: |
            packages/desktop/src-tauri/target/release/bundle/dmg/*.dmg
            packages/desktop/src-tauri/target/release/bundle/macos/*.app
          retention-days: 30
          if-no-files-found: warn

      - name: Build and Release (tags only)
        if: startsWith(github.ref, 'refs/tags/')
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          projectPath: packages/desktop
          tagName: ${{ github.ref_name }}
          releaseName: 'OmniGrab ${{ github.ref_name }}'
          releaseBody: 'macOS version build.'
          releaseDraft: true
          prerelease: false
