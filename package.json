{
  "name": "@omnigrab/monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:ui": "npm --prefix packages/ui run build:desktop",
    "dev:desktop": "npm --prefix packages/desktop run dev",
    "build:desktop": "npm --prefix packages/desktop run build",
    "dev:android": "cd packages/android && .\\.venv\\Scripts\\Activate.ps1 && python -m briefcase dev"
  }
}
